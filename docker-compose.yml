version: "3.8"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      FLASK_ENV: production
      FLASK_APP: run.py
      PORT: 5000
    ports:
      - "5000:5000"  # single container; point proxy to this
    restart: unless-stopped
